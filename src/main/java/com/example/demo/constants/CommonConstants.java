package com.example.demo.constants;

public interface CommonConstants {

    interface DictType {
        String CODENO = "codeNo";
        String ENUM = "enum";
        String TABLE = "tab";
    }
}
